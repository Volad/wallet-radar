package com.walletradar.domain.transaction.normalized;

/**
 * Classification signal for normalized transaction semantics, independent from pricing resolution.
 */
public enum ClassificationStatus {
    CONFIRMED,
    NEEDS_REVIEW
}
