package com.walletradar.domain.auth;

/**
 * Supported identity providers for SSO.
 * Extensible: add new providers here and implement corresponding OAuth2 client registration.
 */
public enum IdentityProvider {
    GOOGLE
}
