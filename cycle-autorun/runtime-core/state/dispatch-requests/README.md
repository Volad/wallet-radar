# dispatch requests

This directory stores machine-readable dispatch request envelopes produced by `command-runner.js`.

An external adapter may consume these files and deliver prompts to actual agent sessions.

