The pipeline is waiting for user input.

Open question id: `{{questionId}}`
Asked by: `{{fromRole}}`
Cycle: `{{cycle}}`

Question:
{{questionText}}

Context:
{{questionContext}}

Record the answer back into:
`{{questionLogPath}}`
