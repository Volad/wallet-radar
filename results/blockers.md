| ID | status | description | evidence |
|---|---|---|---|
| B-16 | RESOLVED | The earlier explorer transfer-row contamination that produced bogus native bridge legs is no longer the dominant live risk. The representative bridge rows were rechecked and the old false native legs are gone. | representative rechecks: `0xd2cdbd7a...`, `0x27978f7b...`; only `2` `BRIDGE_IN` rows still carry native legs and both are supported by real raw transfers (`0x82618972...`, `0x9187f4ca...`) |
| B-17 | RESOLVED | `BSC` provider-first ingestion is now complete for the current wallet universe. The active wallet raw count matches live Ankr provider count exactly. | provider counts: `0x1a87...=33`, `0xf03b...=0`, `0x68bc...=0`; persisted `BSC raw=33`; missing count `0` |
| B-18 | OPEN | Clarification reason hygiene is wrong in the live snapshot. The queue itself is mostly valid, but the persisted reasons do not describe the real missing fields. | `PENDING_CLARIFICATION=300`; raw-side missing `txreceipt_status=300`; raw-side missing `effectiveGasPrice=300`; actual create rows `0`; persisted `MISSING_CONTRACT_ADDRESS=300`; spurious contract-address reason `300` |
| B-19 | OPEN | `NEEDS_REVIEW` is now dominated by a small set of repeatable families rather than random noise. The main unresolved groups are promo/phishing families, zero-amount no-op families, and method-aware router overloads. | `PROMO_SPAM_PHISHING=131`; `ZERO_AMOUNT_TOKEN_TRANSFER=38`; `CLASSIFICATION_FAILED=28`; `ROUTER_METHOD_OVERLOAD_UNSUPPORTED=21`; top router clusters on Base `0xc6a2...`, Optimism `0x416b...`, zkSync `0xdaee...`, Arbitrum `0x5e09...` |
| B-20 | RESOLVED | The main Merkl claim family on `0x3ef3...` is effectively closed in the live snapshot. Remaining review rows in this family are explainable `CLAIM_WITHOUT_MOVEMENT` per-wallet cases, not broad classifier failure. | `REWARD_CLAIM` counts: Avalanche `12`, Katana `10`, Arbitrum `10`, Plasma `4`, Unichain `3`, Ethereum `2`; remaining review rows are specific `CLAIM_WITHOUT_MOVEMENT` samples such as `0xf13356fe...` |
| B-21 | OPEN | Product policy for zero-amount / no-op token families is still unresolved. These rows are financially non-economic but are not yet normalized into a final documented terminal type. | `ZERO_AMOUNT_TOKEN_TRANSFER=38`; dominant families: Arbitrum `0x0cf79e0a=10`, Avalanche `a9059cbb=10`, Avalanche `0x12514bba=8`, Base `0x12514bba=7` |
| B-22 | OPEN | Selector-recovery parity is improved but not closed. A small but non-trivial set of current review rows is still recoverable from persisted calldata without any new external evidence. | empty `rawData.methodId=1230`; recoverable from `input[0:10]=856`; current review rows still recoverable from persisted calldata `24` |
| B-23 | OPEN | Method-aware router overload families remain under-classified. Some are strong candidates for LP exit / fee-claim closure from persisted raw, while others still lack enough persisted evidence and should remain explicit review. | Base `0xc6a2... + 0xac9650d8 = 8`; Optimism `0x416b... + 0xac9650d8 = 6`; zkSync `0xdaee... + 0x3593564c = 4`; Arbitrum `0x5e09... + 0xac9650d8 = 2` |
