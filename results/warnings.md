# Current warnings

- Archived `auto-loop-handoff/artifacts/cycle.zip` artifacts predate this fresh live snapshot and are not authoritative for cycle 67.
- `external_ledger_raw = 0`, so broader-goal Bybit family reconstruction cannot be completed raw-first on this DB snapshot.
- `protocolName` is best-effort metadata, not an accounting rule by itself.
- `counterpartyAddress` must stay separate from `matchedCounterparty` and `correlationId`.
